import { View, StyleSheet } from 'react-native';

export const Button = () => {
    return (
        <View>

        </View>
    )
}

const styles = StyleSheet.create(
    {
        button: {
            corn
        }
    }
)